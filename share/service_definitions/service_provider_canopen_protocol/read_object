services:
  service_provider_canopen_protocol/read_object:
    request:
      index: { type: uint16_t }
    response:
      data_type: { type: uint16_t }
      objcode: { type: uint8_t }
      max_subindices: { type: uint8_t }
      name: { type: string }
      error_message: { type: string, default: "no error" }

