services:
  - name: service_provider_canopen_protocol/write_element
    request:
      - { name: index, type: uint16_t }
      - { name: sub_index, type: uint8_t, default: 0 }
      - { name: value, type: string }
    response:
      - { name: error_message, type: string, default: "no error" }
