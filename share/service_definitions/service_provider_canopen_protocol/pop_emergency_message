services:
  service_provider_canopen_protocol/pop_emergency_message:
    response:
      timestamp_sec: { type: uint64_t }
      timestamp_nsec: { type: uint64_t }
      error_code: { type: uint16_t }
      error_register: { type: uint8_t }
      data: { type: uint8_t, array: true }
      error_message: { type: string, default: "no error" }

